<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:dubbo="http://code.alibabatech.com/schema/dubbo"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans-4.3.xsd
        http://code.alibabatech.com/schema/dubbo
        http://code.alibabatech.com/schema/dubbo/dubbo.xsd">

    <!-- 消费方应用名，用于计算依赖关系，不是匹配条件，不要与提供方一样 -->
    <dubbo:application name="consumer-of-helloworld-app"  />

    <!-- 使用multicast广播注册中心暴露发现服务地址 -->
    <!-- <dubbo:registry address="multicast://224.5.6.7:1234" /> -->
    <dubbo:registry address="zookeeper://127.0.0.1:2181?client=zkclient" />

    <!-- 生成远程服务代理，可以和本地bean一样使用demoService -->
    <dubbo:reference id="demoService" interface="com.csy.dubbo.provider.DemoService" timeout="20000">
        <dubbo:method name="sayHello" async="true"/>  <!-- 如果方法没有配置异步方法，则future为空 -->
    </dubbo:reference>

    <!-- 异步同步是针对方法级的，一个接口中可以既包含同步方法和异步方法 -->
    <dubbo:reference id="demoServiceV2" interface="com.csy.dubbo.provider.DemoServiceV2" timeout="20000"/>

</beans>